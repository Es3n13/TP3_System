apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-processor-h5
  labels:
    app: user-processor-h5
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-processor-h5
  template:
    metadata:
      labels:
        app: user-processor-h5
    spec:
      containers:
        - name: user-processor-h5
          image: rasmile/user-processor-h5:latest
          imagePullPolicy: Always
          env:
            - name: MYSQL_DSN
              value: "root:pass@tcp(mysql:3306)/uqar"
            - name: KAFKA_BROKER
              value: "kafka-0.kafka:9092"
            - name: KAFKA_INPUT_TOPIC
              value: "users"
            - name: KAFKA_OUTPUT_TOPIC
              value: "notification"
            - name: KAFKA_GROUP_ID
              value: "user-processor-h5-group"
          resources:
            requests:
              cpu: "300m"
              memory: "256Mi"
            limits:
              cpu: "2000m"
              memory: "1Gi"
