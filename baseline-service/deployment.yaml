apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-processor
  labels:
    app: user-processor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-processor
  template:
    metadata:
      labels:
        app: user-processor
    spec:
      containers:
        - name: user-processor
          image: rasmile/user-processor:latest
          imagePullPolicy: Always
          env:
            - name: MYSQL_DSN
              value: "root:pass@tcp(mysql:3306)/uqar"
            - name: KAFKA_BROKER
              value: "kafka-0.kafka:9092"
            - name: KAFKA_INPUT_TOPIC
              value: "users"
            - name: KAFKA_OUTPUT_TOPIC
              value: "notification"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"